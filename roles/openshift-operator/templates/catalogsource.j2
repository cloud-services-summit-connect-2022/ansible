apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: "{{ operator_catalogsource_name }}"
  namespace: "{{ operator_catalogsource_namespace }}"
spec:
  sourceType: grpc
  image: "{{ operator_catalogsource_image }}:{{ operator_catalogsource_image_tag }}"
  displayName: "{{ install_operator_catalogsource_name }}"
  publisher: "Red Hat AgnosticD"
{% if operator_catalogsource_pullsecrets | length > 0 %}
  secrets: {{ operator_catalogsource_pullsecrets | to_json }}
{% endif %}
